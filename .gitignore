# build output — styles.css is assembled from styles/ by styles-assemble.mjs
main.js
styles.css
*.js.map

# the test vault `npm run test-build` writes into this repository
.obsidian/

# the browser page `npm run harness` builds
.harness/

# a hand-drawn mock of a projection that does not exist yet, bundled by
# `npm run harness -- test/harness/mock.ts`. Uncommitted on purpose: nothing imports it,
# so `npm run analyze` reports it dead, which is the right answer for a scratch file and
# the wrong one to suppress. See test/CLAUDE.md, "Looking at it".
test/harness/mock.ts

# the report `npm run health` writes
.health/

# dependencies
node_modules/

# misc
.DS_Store
data.json
coverage*/

# release artifacts
dist/

# subagent-driven-development scratch: ledgers, briefs, review packages
.superpowers/

# the throwaway git worktrees an isolated subagent runs in
.claude/worktrees/

docs/absences
